# DEPRECATED — removed. Use incident_commander.elastic_client instead.
# This file exists only as a tombstone to prevent stale imports.
raise ImportError(
    "src.elastic_client is removed. Use: from incident_commander.elastic_client import AgentBuilderClient"
)
